// css
import classes from "./ExperimentBar.module.css";

const ExperimentBar = () => {
  return (
    <div className={classes["experiment-bar"]}>
      <h1></h1>
    </div>
  );
};

export default ExperimentBar;
