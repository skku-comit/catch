// css
import classes from "./Characters.module.css";

const Characters = () => {
  return (
    <div className={classes["character-container"]}>character container</div>
  );
};

export default Characters;
